app.controller('rootCtr', function ($scope, service) {
    $scope.init = function () {

    }

    $scope.init();
});

app.controller('welcomeCtr', function ($scope, service) {
    $scope.init = function () {
    }
    $scope.init();
});